<turbo-stream action="replace" targets=".ts-qotd-{{ qotd.id }}">
    <template>
        {{ include('qotd/_qotd.html.twig', { qotd: qotd, section: section ?? 'year' })}}
    </template>
</turbo-stream>

<turbo-stream action="append" target="toasts">
    <template>
        {% embed 'helper/notification.html.twig' %}
            {% block title '✅ Saved!' %}
            {% block body 'Thanks for your vote!' %}
        {% endembed %}
    </template>
</turbo-stream>
